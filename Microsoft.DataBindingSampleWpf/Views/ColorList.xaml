<Window x:Class="Microsoft.DataBindingSampleWpf.Views.ColorList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Microsoft.DataBindingSampleWpf.Views"
        xmlns:res="clr-namespace:Microsoft.DataBindingSampleWpf.Resources"
        mc:Ignorable="d"
        Title="ColorList" Height="450" Width="800">
    <Window.DataContext>
        <res:ColorListDataContext/>
    </Window.DataContext>
    <Grid>
        <ListBox ItemsSource="{Binding LotsOfColors}" Margin="20" Width="200" HorizontalAlignment="Left" VerticalAlignment="Top">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Rectangle Width="30" Height="30">
                            <Rectangle.Fill>
                                <SolidColorBrush Color="{Binding Color}"/>
                            </Rectangle.Fill>
                        </Rectangle>
                        <TextBlock Text="{Binding Name}" Margin="20, 10, 0, 10"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
