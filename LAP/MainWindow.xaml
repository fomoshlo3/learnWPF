<Window x:Class="LAP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LAP"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition x:Name="NavBar"  Height="Auto"/>
            <RowDefinition Height="*"/>
          
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
           
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="5" Grid.RowSpan="1">
            <Button Command="{Binding NavigateToQueryTabViewCommand}" Content="Manage"/>
            <Button Command="{Binding NavigateToSettingsViewCommand}" Content="Settings"/>
        </StackPanel>
        <ContentControl Grid.Row="1" Grid.Column="1" Content="{Binding Navigation.CurrentViewModel}"/>
    </Grid>
</Window>
